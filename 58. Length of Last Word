class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        list1=list(s.strip().split())
        print(list1)
        result=len(list1[len(list1)-1])
        return result
